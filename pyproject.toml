[project]
name = "py-chat"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "lilyco-42", email = "liuqingffffff@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
py-chat = "py_chat:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
